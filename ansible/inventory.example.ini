[statuspulse]
# ansible_user is the initial Ubuntu SSH user for the fresh VM.
# Keep ansible_port aligned with deploy_ssh_port in vars.yml.
statuspulse-vm ansible_host=44.202.54.78 ansible_user=ubuntu ansible_port=22
