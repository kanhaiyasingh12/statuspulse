[statuspulse]
# ansible_user is the initial Ubuntu SSH user for the fresh VM.
# deploy_ssh_port in vars.yml becomes the hardened SSH port after the first run.
statuspulse-vm ansible_host=YOUR_SERVER_IP ansible_user=ubuntu ansible_port=22
